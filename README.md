# reqx — API latency & performance tester

A lightweight Next.js + TypeScript app for benchmarking HTTP endpoints with realtime charts.

Quick start:

```bash
npm install
npm run dev
```

Open http://localhost:3000

Features:
- Sequential & concurrent request runs
- Streamed results, realtime charts
- Export results to JSON/CSV
- History saved in localStorage

